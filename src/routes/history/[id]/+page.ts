// Game ids only exist in this device's localStorage, so there is nothing for the build to
// enumerate. The adapter's `index.html` fallback serves this route and the client resolves it.
export const prerender = false;
